package com.knoldus.cartmgmt.data.model

case class UserAccount(
                        userId: Int,
                        userName: String,
                        balance: Double,
                      )
